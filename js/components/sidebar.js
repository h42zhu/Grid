import React from 'react'
import ReactDOM from 'react-dom'

class SidebarComponent extends React.Component {
    render() {
        
        var itemList = (<li className={}
                            onClick={}>  </li>)
        
        <div className="sidebar">
            <ul className={}>
                {itemList}
            </ul>
        </div>

    }
    
    
}

export default SidebarComponent